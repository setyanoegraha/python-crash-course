# Assign a value to the variable 'person_name'
person_name = "Fernando rezian hover"

# Print out the 'person_name' variable in lowercase
print(person_name.lower())

# Print out the 'person_name' variable in uppercase
print(person_name.upper())

# Print out the 'person_name' variable in title case
print(person_name.title())

"""
Explanation:

1️⃣ We assign the string `"Fernando rezian hover"` to the variable `person_name`.
2️⃣ The `print(person_name.lower())` statement prints the name in **all lowercase**.
3️⃣ The `print(person_name.upper())` statement prints the name in **all uppercase**.
4️⃣ The `print(person_name.title())` statement prints the name in **title case**, where each word's first letter is capitalized.

✅ Key concepts in this exercise:
   - **String assignment:** Storing a person's name in a variable.
   - **String methods:**
     - `.lower()` converts all letters to lowercase.
     - `.upper()` converts all letters to uppercase.
     - `.title()` capitalizes the first letter of each word.

📌 This is useful for formatting names, user inputs, and displaying text properly in different cases.
"""
