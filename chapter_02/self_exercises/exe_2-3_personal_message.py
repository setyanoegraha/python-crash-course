# Assign a value to the variable 'name'
name = "George"

# Crete a formatted personal string message using f-string
message = f"Good Morning! Mr.{name}, Would you like a cup of coffee while you learn python?"

# Print out the formatted message
print(message)

"""
Explanation:

1️⃣ We assign the string `"George"` to the variable `name`.
2️⃣ We create a message using an f-string (`f""`) to include the value of `name` within the sentence dynamically.
3️⃣ The `print(message)` statement outputs the formatted message with the name included.

✅ Key concepts in this exercise:
   - **Variable assignment:** Storing a name inside a variable.
   - **String formatting with f-strings:** Embedding variables inside a string for dynamic output.
   - **Printing strings:** Using `print()` to display the final formatted message.

📌 This technique is useful for dynamically generating personalized messages in applications.
"""
