# Create a list of names
names = ['john', 'greg', 'sheila', 'alisha', 'megan']

# Print each name with the first letter capitalized
print(names[0].title())  # Prints 'John'
print(names[1].title())  # Prints 'Greg'
print(names[2].title())  # Prints 'Sheila'
print(names[3].title())  # Prints 'Alisha'
print(names[4].title())  # Prints 'Megan'

"""
Explanation: 

1️⃣ We create a list called 'names' containing five lowercase names.
2️⃣ Each name is accessed using its index in the list (starting from 0).
3️⃣ The `.title()` method capitalizes the first letter of the name.
4️⃣ The `print()` function outputs the modified names to the console.

✅ Key concepts:
    - **Lists:** A way to store multiple in a single variable.
    - **Indexing:** Elements in a list are accessed using their index (0-based).
    - **String formatting:** The `.title()` method ensure proper capitalization.
    - **Printing Output:** `print()` is used to display the formatted names.

🚀 This technique is useful when working with user input, name formatting, or data presentation
"""
